# claimgraph live store + local artifacts (the committable artifacts are
# `claim dump` output and .claimgraph/config.json)
.claimgraph/db/
.claimgraph/db.lock
.claimgraph/db.curate.lock
.claimgraph/db.capture.log
.claimgraph/db.curate.log
.claimgraph/db.evidence/
.claimgraph/db.oplog/
.claimgraph/db.retrievals
.claimgraph/db.version
*.dump.jsonl
bench/results/

# Book build artifacts (sources live in book/chapters/; `bb book` regenerates these)
book/rendered/
.clay/
.cpcache/

# clj-kondo lint cache (imports/ stays tracked: it pins the analyzed lib configs)
.clj-kondo/.cache/
